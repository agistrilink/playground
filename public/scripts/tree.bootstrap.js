/*
 * 
 */

define([
        'jquery',
        'bootstrap',
        'utils/jquery.debug'
	],
	function($) {
	
	
	
	return {
		run: function() {

		
		
			alert('bootstrap no syntax error');
/*	
			var user = User.newInstance('harrold korte');
			
			alert('user.url() = ' + user.url());
			
			var mimeTypeJson = function(xhr){alert('mimeTypeJson');
				xhr.overrideMimeType("application/json; charset=utf-8");			
			}
			
			user.on("change", function() {
				alert(JSON.stringify(this));
			});	
			user.fetch({
				beforeSend: mimeTypeJson
			}).done(function () {
	//			alert(JSON.stringify(user));
			});
			
			_.each([1, 2, 3], function(num){ alert(num); });
			alert('done');
			
	//		alert(JSON.stringify(user));
*/	
	/*
			$.get(user.url(), function (rsp){
				alert(rsp);
			});
	*/
			
			
	/*		
			user.fetch({
				success: function (user) {
					alert(user.toJSON());
				},
				error: function(user) {
					alert('error: ' + user);
				}
			});
	
			_.each([1, 2, 3], function(num){ alert(num); });
	
	//		['one', 'two', 'three'].each(alert);
	*/
			
			
			
		
	}
}





	}
);
