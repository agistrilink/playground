/*
 * 
 */

define([
        'mod/developer',
        'prototype/array'
	],
//	['jquery', 'mod/spine/developer', 'mod/spine/employee', 'mod/spine/user', 'object'],
	function(Developer) {
		alert('no syntax error');
//		alert(User.newInstance('harrold korte').toString());
		
//		alert(User.newInstance('korte').toString());
		
//		alert(Employee.newInstance('dev', 'harrold korte').toString());
		
//		alert(Developer.newInstance('harrold korte', 'game1-4').toString());
		['one', 'two', 'three'].each(alert);
	}
);
